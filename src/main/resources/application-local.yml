# Local dev profile: matches docker-compose.yml published ports.
# Run with: java -jar ... --spring.profiles.active=local
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smartship_shore?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: shore
    password: shore123

shore:
  mqtt:
    broker-url: tcp://localhost:1883
  kafka:
    # Host-mapped KRaft listener from docker-compose.yml.
    bootstrap-servers: localhost:29092
