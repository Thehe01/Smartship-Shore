# Copy to .env and adjust before `docker compose up -d`. Never commit real secrets.
MYSQL_ROOT_PASSWORD=root123
MYSQL_DB=smartship_shore
MYSQL_USER=shore
MYSQL_PASS=shore123

# Shore app overrides (optional; defaults in application.yml / application-local.yml apply)
# KAFKA_BOOTSTRAP_SERVERS=localhost:29092
# MQTT_BROKER_URL=tcp://localhost:1883
